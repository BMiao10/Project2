
import pytest
from cluster import algs

def test_partitioning():
	assert True

def test_hierarchical():
	assert True

def test_clustSimilarity():
	assert True

def test_clustQuality():
	# check that single 

	# check that single 
	assert True

def test_ligandIO():
	ligands = readLigands("./ligand_information.csv")
	ligand1 = ligands[0]
	ligand2 = ligands[-1]

	# check that the ligand onbits are densified correctly
	assert()
	assert()

	# assert that the on bits are in fact on
	assert()
	assert()

	# assert that the not on bits are indeed off
	assert()
	assert()

	
	


